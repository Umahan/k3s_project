apiVersion: v1 
kind: Service
metadata: 
  name: node-exporter-service
  namespace: monitoring
spec:
  selector: 
    app: node-exporter
  ports:
  - name: http
    port: 9100
    targetPort: 9100
  type: NodePort # ClusterIp, если доступ внутри кластера
